{
  "github": {
    "silent": true,
    "autoJobCancelation": true
  },
  "ignoreCommand": "git diff --quiet HEAD~1 HEAD -- src/ package.json next.config.ts tsconfig.json public/ vercel.json && exit 0 || exit 1"
}
